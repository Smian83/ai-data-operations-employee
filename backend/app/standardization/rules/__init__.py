"""Per-field-type-family rule modules. See each submodule's docstring;
app.standardization.engine owns sequencing/orchestration -- these modules
only expose small, individually-testable primitives."""
