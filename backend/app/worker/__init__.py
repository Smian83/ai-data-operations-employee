"""Module 4: the task execution engine (worker pool, reaper, credential
resolution, execution handlers). Runs as a separate process from the
FastAPI app -- see app.worker.runner / `python -m app.worker`."""
