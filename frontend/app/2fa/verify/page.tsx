"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";

export default function TwoFactorVerifyPage(){const router=useRouter();const [recovery,setRecovery]=useState(false);const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setError("");setLoading(true);const data=new FormData(event.currentTarget);try{const response=await fetch(recovery?"/api/auth/2fa/recovery":"/api/auth/2fa/verify",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(recovery?{recovery_code:data.get("recovery_code")}:{code:data.get("code")})});const body=await response.json().catch(()=>null);if(!response.ok){setError(body?.detail??"Verification failed.");return}router.replace("/");router.refresh()}catch{setError("Unable to reach the authentication service.")}finally{setLoading(false)}}return <main className="login-shell"><section className="login-card card"><div className="login-mark" aria-hidden="true">2FA</div><p className="login-eyebrow">Identity verification</p><h1>{recovery?"Use a recovery code":"Enter your code"}</h1><p className="login-copy">{recovery?"Recovery codes are single-use.":"Open your authenticator app to continue."}</p><form className="login-form" onSubmit={submit}><label>{recovery?"Recovery code":"6-digit authentication code"}<input className="ops-input code-input" name={recovery?"recovery_code":"code"} inputMode={recovery?"text":"numeric"} autoComplete="one-time-code" pattern={recovery?undefined:"[0-9]{6}"} maxLength={recovery?32:6} required disabled={loading}/></label>{error&&<div className="login-error" role="alert">{error}</div>}<button className="glow-button login-submit" disabled={loading}>{loading?"Verifying…":"Verify"}</button></form><button className="auth-link-button" type="button" onClick={()=>{setRecovery(!recovery);setError("")}}>{recovery?"Use authenticator code":"Use a recovery code"}</button></section></main>}
